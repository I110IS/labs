{
  "name": "ids-labs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild template.js --bundle --outfile=dist/template.js --loader:.html=text --loader:.woff=file --loader:.eot=file --loader:.ttf=file"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.15.1",
    "reveal.js": "^4.3.1"
  }
}
