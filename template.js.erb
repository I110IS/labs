<%# Local variables:
  node_modules: Path to the node_modules folder.
  theme: Theme name from reveal.js %>
import Reveal from '<%= node_modules %>/reveal.js';
import Markdown from '<%= node_modules %>/reveal.js/plugin/markdown/markdown.esm.js';
import '<%= node_modules %>/reveal.js/dist/reveal.css';
import '<%= node_modules %>/reveal.js/dist/theme/<%= theme %>.css';

let deck = new Reveal({
   plugins: [ Markdown ]
})

deck.initialize();
